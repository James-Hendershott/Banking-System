<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Transfer Funds</title>
    <link rel="stylesheet" href="css/bootstrap.min.css" />
    <link rel="stylesheet" href="/bootstrap-icons.css" />
  </head>
  <body>
    <!-- Navbar -->
    <% include partials/navbar %>

    <!-- Transfer Form -->
    <div class="container mt-5">
      <h2 class="text-center">Transfer Funds</h2>

      <!-- Success Message -->
      <% if (typeof success !== 'undefined') { %>
      <div class="alert alert-success text-center" role="alert">
        <%= success %>
      </div>
      <% } %>

      <!-- Error Message -->
      <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-danger text-center" role="alert">
        <%= error %>
      </div>
      <% } %>

      <form action="/transfer" method="POST">
        <div class="form-group mb-3">
          <label for="fromAccount" class="form-label">From Account</label>
          <select id="fromAccount" name="fromAccount" class="form-control" required>
            <% accounts.forEach(account => { %>
            <option value="<%= account.account_id %>"><%= account.account_type %> - $<%= account.balance %></option>
            <% }); %>
          </select>
        </div>
        <div class="form-group mb-3">
          <label for="toAccount" class="form-label">To Account</label>
          <select id="toAccount" name="toAccount" class="form-control" required>
            <% accounts.forEach(account => { %>
            <option value="<%= account.account_id %>"><%= account.account_type %> - $<%= account.balance %></option>
            <% }); %>
          </select>
        </div>
        <div class="form-group mb-3">
          <label for="amount" class="form-label">Amount</label>
          <input
            type="number"
            id="amount"
            name="amount"
            class="form-control"
            step="0.01"
            required
          />
        </div>
        <div class="form-group mb-3">
          <label for="memo" class="form-label">Memo (Optional)</label>
          <input type="text" id="memo" name="memo" class="form-control" />
        </div>
        <button type="submit" class="btn btn-primary w-100">
          <i class="bi bi-arrow-right-circle"></i> Transfer
        </button>
      </form>

      <!-- Back to My Accounts Button -->
      <div class="text-center mt-4">
        <a href="<%= backLink %>" class="btn btn-secondary">
          <i class="bi bi-arrow-left"></i> Back to My Accounts
        </a>
      </div>
    </div>
  </body>
</html>
